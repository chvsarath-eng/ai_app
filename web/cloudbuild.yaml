# ============================================
# Cloud Build Configuration for img2x-web
# ============================================
# Triggers on push to main branch
# Builds Docker image and deploys to Cloud Run

steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE_NAME}:${COMMIT_SHA}'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE_NAME}:latest'
      - '.'
    dir: 'web'

  # Step 2: Push the Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE_NAME}'

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE_NAME}:${COMMIT_SHA}'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '3000'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--set-env-vars'
      - 'DODO_PAYMENTS_ENVIRONMENT=${_DODO_PAYMENTS_ENVIRONMENT},DODO_PRODUCT_DIGITAL_ID=${_DODO_PRODUCT_DIGITAL_ID},DODO_PRODUCT_HARDCOVER_ID=${_DODO_PRODUCT_HARDCOVER_ID},DODO_PRODUCT_SHIPPING_ID=${_DODO_PRODUCT_SHIPPING_ID}'
      - '--set-secrets'
      - 'SMTP_HOST=smtp-host:latest,SMTP_PORT=smtp-port:latest,SMTP_USER=smtp-user:latest,SMTP_PASS=smtp-pass:latest,STORY_SERVICE_URL=story-service-url:latest,STORY_INVOKER_CREDENTIALS_JSON=story-invoker-credentials:latest,DODO_PAYMENTS_API_KEY=dodo-payments-api-key:latest,DODO_PAYMENTS_WEBHOOK_KEY=dodo-payments-webhook-key:latest'

# Substitution variables (can be overridden in trigger)
substitutions:
  _REGION: 'us-central1'
  _REPO: 'img2x-repo'
  _SERVICE_NAME: 'img2x-web'
  _DODO_PAYMENTS_ENVIRONMENT: 'live_mode'
  _DODO_PRODUCT_DIGITAL_ID: 'product_digital'
  _DODO_PRODUCT_HARDCOVER_ID: 'product_hardcover'
  _DODO_PRODUCT_SHIPPING_ID: 'product_shipping'

# Secrets from Secret Manager
# Build options
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

# Build timeout (10 minutes)
timeout: '600s'

# Store images in Artifact Registry
images:
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE_NAME}:${COMMIT_SHA}'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE_NAME}:latest'
